import styles from './style.module.css';

const Lecture = () => {
    return (
        <>
        <section className={styles.goldie}>
            So, in today's section, we'd be discussing the importance of React.js, and how fast the technology is growing in today's web development.
        </section>
        </>
    );
}

export default Lecture;